{% extends 'base.html.twig' %}

{% block title %}NomTipoServicio{% endblock %}

{% block body %}
    <h1>NomTipoServicio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nom_tipo_servicio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ nom_tipo_servicio.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nom_tipo_servicio_index') }}">back to list</a>

    <a href="{{ path('nom_tipo_servicio_edit', {'id': nom_tipo_servicio.id}) }}">edit</a>

    {{ include('nom_tipo_servicio/_delete_form.html.twig') }}
{% endblock %}