{% extends 'base.html.twig' %}

{% block title %}NomTipoPersona index{% endblock %}

{% block body %}
    <h1>NomTipoPersona index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for nom_tipo_persona in nom_tipo_personas %}
            <tr>
                <td>{{ nom_tipo_persona.id }}</td>
                <td>{{ nom_tipo_persona.nombre }}</td>
                <td>
                    <a href="{{ path('nom_tipo_persona_show', {'id': nom_tipo_persona.id}) }}">show</a>
                    <a href="{{ path('nom_tipo_persona_edit', {'id': nom_tipo_persona.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('nom_tipo_persona_new') }}">Create new</a>
{% endblock %}