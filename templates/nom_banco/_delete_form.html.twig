<form method="post" action="{{ path('nom_banco_delete', {'id': nom_banco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nom_banco.id) }}">
    <button class="btn">Delete</button>
</form>