{% extends 'base.html.twig' %}

{% block title %}NomProvincia{% endblock %}

{% block body %}
    <h1>NomProvincia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nom_provincium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ nom_provincium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nom_provincia_index') }}">back to list</a>

    <a href="{{ path('nom_provincia_edit', {'id': nom_provincium.id}) }}">edit</a>

    {{ include('nom_provincia/_delete_form.html.twig') }}
{% endblock %}